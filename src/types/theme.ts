export type ThemeProps = {
  colors: {
    primary: string,
    secondary: string,
    veryVarkBlue: string
    darkGrayishBlue: string
    grayishBlue: string
    lightGrayishBlue: string
    white: string
    black: string
  }
}
